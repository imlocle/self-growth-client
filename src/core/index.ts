/**
 * Core Module
 *
 * Centralized export for all core utilities and configuration.
 *
 * @example
 * ```typescript
 * import { apiClient, ENV } from '@core';
 * ```
 */

export { apiClient } from "./network/apiClient";
export { ENV } from "./config/env";
